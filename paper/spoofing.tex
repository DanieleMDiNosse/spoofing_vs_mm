\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsthm,amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{siunitx}
\usepackage{natbib}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\usepackage{setspace}
\usepackage{subcaption}
\usepackage{placeins}
\geometry{margin=2.5cm}
\usepackage{changepage}
\usepackage{ulem}
\usepackage{authblk}
\usepackage{array}
\usepackage[table]{xcolor}
\usepackage{tabularx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

\onehalfspacing
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue,
    pdftitle={Detecting Spoofing in Electronic Markets},
    pdfauthor={Author Name}
}


\title{Market Microstructure Frameworks for Spoofing Detection: Isolating Manipulative Intent from Legitimate Liquidity Provision}
\author[1]{Daniele Maria Di Nosse\textsuperscript{\dag}}
\author[2]{Francesco Gigante}
\author[2]{Antonio Russo}
\author[1]{Fabrizio Lillo}

\affil[1]{Scuola Normale Superiore, Pisa, Italy}
\affil[2]{Consob, Rome, Italy}
\date{\today}

\begin{document}

\maketitle
\begin{center}
  \footnotesize
  \dag Corresponding author. Email: \texttt{daniele.dinosse@sns.it}
\end{center}

\begin{abstract}
In this paper we detect the bad guys and we find how to tell them they are actually bad guys. Hopefully, we will be able 
to do this without accusing the good guys of being bad guys. 
\end{abstract}

\paragraph{Keywords.}
Spoofing detection; market manipulation; limit order book; high-frequency trading; anomaly detection; market maker; market microstructure

\section{Introduction}
\label{sec:introduction}
The transition of global financial exchanges to fully electronic, continuous double auction matching engines has fundamentally 
transformed the mechanics of price discovery and liquidity provision. In modern financial ecosystems, the Limit Order Book 
(LOB) serves as the central nervous system of the market, aggregating the continuous stream of bid and ask limit orders 
submitted by diverse market participants. While the advent of algorithmic and high-frequency trading (HFT) has undeniably 
narrowed bid-ask spreads and enhanced short-term market efficiency, it has concurrently introduced profound vulnerabilities. 
The microsecond latency environment allows sophisticated algorithms to exploit the structural mechanics of the LOB, 
executing complex manipulative strategies that deceive heuristic-based trading algorithms and human traders alike.
Among these manipulative practices, spoofing represents one of the most pervasive and challenging threats to market 
integrity. Spoofing is a deceptive algorithmic strategy wherein a market participant places a massive, non-bona fide 
order on one side of the LOB with the explicit intent of canceling it before execution. The presence of this immense, 
artificial volume creates a false impression of directional market pressure. This artificial pressure manipulates the 
micro-price and induces other market participants—particularly algorithms that rely on order book imbalance indicators—to 
adjust their quotes or submit market orders. Once the price has been artificially driven to a favorable level, the 
spoofer executes a smaller, bona fide order on the opposite side of the book and immediately withdraws the massive 
deceptive orders, thereby profiting from the artificially induced price dislocation. The European Securities and Markets 
Authority (ESMA), through the Market Abuse Regulation (MAR) \cite{EU5962014}, strictly prohibits such practices, categorizing them as 
behaviors that disrupt the functioning of the trading system and create misleading signals regarding the supply and 
demand of financial instruments. For regulatory and supervisory authorities such as the Commissione Nazionale per le 
Società e la Borsa (CONSOB), the detection and prosecution of spoofing present a severe analytical challenge. The physical 
actions comprising a spoofing strategy—posting limit orders, updating quotes, and rapidly canceling orders—are mechanically 
indistinguishable from the legitimate actions of a bona fide market maker providing liquidity. Both spoofers and market 
makers exhibit exceedingly high order-to-trade ratios (OTTR) and operate at high frequencies. Traditional market 
surveillance mechanisms, which historically relied on static heuristic thresholds, volumetric alerts, or simple cancellation 
ratios, are fundamentally inadequate in this environment. These legacy systems generate an overwhelming volume of false 
positives, frequently flagging legitimate liquidity provision and inventory management as manipulative market abuse.

\section{The Microstructural Divergence: Market Making Versus Spoofing}
\label{sec:mm_vs_spoofing}
To construct a mathematical model capable of detecting market manipulation, it is first necessary to rigorously define 
the divergent objective functions that govern legitimate market making and spoofing. While both participants generate 
massive volumes of limit order messages and cancellations, their underlying economic motivations, risk profiles, and 
optimizations are fundamentally opposed.

\subsection{The Market Maker's Problem}
\label{subsec:market_maker}
A market maker functions is a continuous liquidity provider, simultaneously quoting resting limit orders at the best bid 
and the best ask. The primary objective function of a market maker is to maximize the utility of gains derived from 
capturing the bid-ask spread across a vast number of transactions, while concurrently minimizing the variance of their 
portfolio and strictly managing inventory risk. In market microstructure theory, the pricing dynamics of a liquidity 
provider are governed by the interplay between the arrival rate of market orders and the accumulation of inventory.

The optimization problem for a market maker is frequently modeled using stochastic control techniques and 
Hamilton-Jacobi-Bellman (HJB) equations, where the dealer sets optimal quotes to balance the asymmetric arrival 
of buy and sell orders. 1  If a market maker accumulates a long position (positive inventory), they are exposed 
to the risk of the underlying asset depreciating. To mitigate this inventory risk, the rational market maker will 
their quotes downwards—lowering their ask price to incentivize incoming buyers, and lowering their bid price to 
dissuade incoming sellers. 2  The bid and ask prices are dynamically adjusted functions of the mid-price, the base 
spread required for profitability, and an inventory penalty parameter that increases as the dealer's position deviates 
from neutrality.

Consequently, a market maker's high cancellation rate is entirely reactive and protective. Limit orders are rapidly 
canceled and re-posted to reflect minute changes in the fundamental mid-price, to defend against adverse selection 
informed traders, and to recalibrate 
inventory penalty. The massive volume of orders and cancellations produced by a market maker is therefore characterized 
by symmetry across the LOB and a continuous presence at the top of the book to execute trades. Their expected directional 
gain from posting an order is near zero, as their profit is entirely derived from the spread rather than from inducing 
a directional shift in the mid-price.

\subsection{The Spoofer's Problem}
\label{subsec:spoofing}
In stark contrast, a spoofing agent does not seek to capture the bid-ask spread, nor do they operate as symmetric 
liquidity provider. A spoofer's objective function is purely directional. The spoofer desires to execute a relatively 
small, bona fide order at a highly favorable price by artificially manipulating the micro-price dynamics of the LOB.
To execute this strategy, the spoofer deploys a massive non-bona fide order on the opposite side of the LOB. This 
deliberate injection of spurious volume creates an extreme unbalanced environment, artificially inflating 
the visible depth of the book. In modern electronic markets, countless algorithmic traders utilize strategies that weight 
the volume at the bid against the volume at the ask to predict short-term price distributions. By overwhelming one side 
of the book, the spoofer pollutes these volume imbalance metrics, causing HBL algorithms to mathematically deduce an 
impending price shift and aggressively alter their own quoting behavior.

The critical mathematical dilemma for the spoofer lies in the placement of the non-bona fide order. The spoofer must 
balance the expected market manipulation gain against the explicit risk of unintended execution. If the spoofer places 
the massive non-bona fide order exactly at the top of the book (the best bid or ask), they generate the maximum psychological 
and algorithmic impact on the micro-price. However, doing so exposes the spoofer to massive adverse selection and inventory 
risk; an incoming aggressive market order could inadvertently execute against the spoofer's massive fake order, leaving 
the spoofer holding an enormous, unwanted position.To optimize this expected cost function, sophisticated spoofers rarely 
post their deceptive orders at the absolute best price. Instead, they optimize the posting distance (measured in ticks 
or basis points) deeper within the book. By placing the order at Level 2, Level 3, or beyond, the spoofer ensures the 
volume is still visible to the matching engine's data feed—and thus factored into the volume imbalance metrics of victim 
algorithms—while maintaining a structural buffer against immediate execution. Once the intended directional shift occurs 
and the spoofer's small bona fide order is executed, the massive non-bona fide order is instantly canceled. The spoofer's 
cancellation intent is not to manage inventory, but to terminate the manipulative episode the exact millisecond the cost 
optimization concludes.

\begin{table}[htbp]
\centering
\caption{Core Differentiators for Surveillance Models}
\label{tab:surveillance-models}
\renewcommand{\arraystretch}{1.4}
\setlength{\tabcolsep}{8pt}
\rowcolors{2}{gray!4}{white}

\begin{tabularx}{\textwidth}{
    >{\raggedright\arraybackslash\bfseries}p{0.25\textwidth}
    >{\raggedright\arraybackslash}X
    >{\raggedright\arraybackslash}X
}
\toprule
\rowcolor{gray!15}
\textbf{Microstructure Metric} & \textbf{Legitimate Market Maker} & \textbf{Manipulative Spoofing Agent} \\
\midrule
Primary Objective Function
& Maximize bid--ask spread capture while minimizing inventory variance.
& Minimize execution cost of a directional trade via artificial micro-price impact. \\

Order Book Imbalance
& Generally symmetric, adapting quotes to maintain a neutral inventory.
& Extremely asymmetric, deliberately creating massive, sudden imbalances (\emph{unbalanced quoting}). \\

Cancellation Motivation
& Reactive: mitigating adverse selection and updating quotes to track fair value.
& Proactive: terminating the deceptive posture immediately after a targeted execution. \\

Posting Distance Optimization
& Top of the book (Level 1) to maximize the probability of spread execution.
& Deeper in the book (Level 2/3) to maximize visibility while minimizing execution risk. \\

Expected Directional Gain
& Zero or negative (willing to pay the spread to lay off excessive inventory risk).
& Strictly positive (the massive order statistically subsidizes the execution of the small order). \\
\bottomrule
\end{tabularx}
\end{table}

\section{Existing Approaches}
\label{sec:existing_approaches}
The literature on spoofing detection is still relatively small, but it has moved from transparent microstructure rules
toward sequence and graph models that try to capture richer strategic behavior. For the purpose of this paper, the most
useful distinction is not simply between ``statistical'' and ``machine-learning'' models. It is between methods that
identify a stylized spoofing episode from order-book mechanics, methods that monitor abnormal order-flow states, and
methods that learn temporal or relational patterns from proprietary event data. \Cref{fig:method_taxonomy} summarizes
this taxonomy.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        font=\scriptsize,
        root/.style={
            draw,
            rounded corners=2pt,
            fill=gray!15,
            align=center,
            text width=0.40\textwidth,
            minimum height=0.8cm,
            font=\small\bfseries
        },
        category/.style={
            draw,
            rounded corners=2pt,
            fill=gray!8,
            align=center,
            text width=0.16\textwidth,
            minimum height=0.75cm,
            font=\footnotesize\bfseries
        },
        detail/.style={
            draw,
            rounded corners=2pt,
            fill=white,
            align=left,
            text width=0.17\textwidth,
            minimum height=1.65cm
        },
        arrow/.style={-{Latex[length=2mm]}, thick}
    ]
        \node[root] (root) at (0,0) {Financial \mbox{spoofing} surveillance};

        \node[category] (rules) at (-6.15,-1.45) {Rule-based\\empirical};
        \node[category] (stat) at (-3.075,-1.45) {Statistical\\anomaly};
        \node[category] (seq) at (0,-1.45) {Deep\\sequences};
        \node[category] (graph) at (3.075,-1.45) {Graph-based\\learning};
        \node[category] (expl) at (6.15,-1.45) {Explainable\\forensic};

        \node[detail, below=0.25cm of rules] (rulesd) {\textbullet\ size thresholds\\\textbullet\ distance from touch\\\textbullet\ opposite-side fills\\\textbullet\ price reversal};
        \node[detail, below=0.25cm of stat] (statd) {\textbullet\ multilevel imbalance\\\textbullet\ Wasserstein tests\\\textbullet\ physics momentum\\\textbullet\ spoofability score};
        \node[detail, below=0.25cm of seq] (seqd) {\textbullet\ event streams\\\textbullet\ GRU early warning\\\textbullet\ short-horizon labels};
        \node[detail, below=0.25cm of graph] (graphd) {\textbullet\ transaction graphs\\\textbullet\ community features\\\textbullet\ dynamic latent graphs};
        \node[detail, below=0.25cm of expl] (expld) {\textbullet\ message-level views\\\textbullet\ interpretable scores\\\textbullet\ case reconstruction};

        \foreach \target in {rules,stat,seq,graph,expl} {
            \draw[arrow] (root.south) -- (\target.north);
        }
        \foreach \source/\target in {rules/rulesd,stat/statd,seq/seqd,graph/graphd,expl/expld} {
            \draw[arrow] (\source.south) -- (\target.north);
        }
    \end{tikzpicture}
    \caption{Method families in the spoofing-detection literature. The categories are not mutually exclusive, but they
    highlight the main modeling choice made by each strand of work.}
    \label{fig:method_taxonomy}
\end{figure}

Early empirical papers define spoofing through economically interpretable order-book conditions. The seminal study by
\cite{LeeEomPark2013} identifies large orders placed far from the best quote, followed by opposite-side trading and
subsequent withdrawal, using account-level data from the Korea Exchange. Similar surveillance logic appears in Brazilian
equities \cite{MendoncaDeGenaro2020}, FX spot markets \cite{StenforsSusai2021}, cross-market FX settings
\cite{StenforsEtAl2023}, and Taiwan derivatives \cite{ChenHsiehYang2026}. These studies are valuable because the alert
logic can be explained to supervisors: suspicious activity is tied to order size, posting distance, cancellation timing,
opposite-side execution, and short-lived price impact. Their main limitation is that thresholds are venue-specific and
often depend on privileged message data or account identifiers.

A second line develops statistical and anomaly-oriented monitoring. \cite{TaoDayLingDrapeau2022} derive spoofing
incentives from multilevel order-book imbalance and propose Wasserstein-distance monitoring for real-time detection.
\cite{LiPolukarovaVentre2023} use a statistical-physics representation of the order book to detect abnormal momentum in
crypto markets. More recent explainable approaches also fall partly in this family: forensic visualization reconstructs
message-level spoofing episodes in prosecuted cases \cite{DebieEtAl2023}, while interpretable probabilistic neural
networks estimate the conditional gain from manipulating a centralized crypto order book \cite{FabreChallet2025}. These
methods improve on pure threshold rules by modeling the state of the book, but many papers still report limited
benchmark metrics or rely on non-public datasets.

Machine-learning work mainly adds temporal or relational structure. \cite{TuccellaNadlerSerban2021} formulate spoofing
as an early-warning problem and train a GRU model on cryptocurrency order-book event streams. Graph-based methods then
move the unit of analysis from isolated orders to networks of traders and transactions, which is important for detecting
coordinated or ``conspiracy'' spoofing \cite{KangMuNing2023,KangMuNing2024,XiangEtAl2025}. The strongest reported graph
models combine temporal transaction graphs, community-level features, and dynamic latent representations, but their
practical value is difficult to assess without open labels and chronological cross-market validation.

Overall, the literature suggests two requirements for a robust surveillance model. First, the features should remain
microstructure-grounded: a detector should know whether suspicious volume is large, distant from the touch, rapidly
cancelled, and conditional on an opposite-side execution. Second, the evaluation should respect the enforcement setting:
labels are noisy, true positives are rare, and false positives impose substantial analyst costs. This motivates the
agent-level, spatio-temporal framework developed below, which keeps the interpretability of rule-based surveillance while
using the full order-flow sequence available in proprietary data.

\section{Data}
\label{sec:data}

\section{Model}
\label{sec:model}
In this section we provide the base reasoning and metrics used to distinguish between legitimate market making behaviour
and possible spoofing strategies.

% \subsection{Spoofer's timeline}
% \label{subsec:spoofing_timeline}
% \begin{enumerate}
%     \item The spoofer posts a large non bona fide order far away from the best quote and, simultaneously, a small bona 
%     fide order close to the best quote
%     \item The spoofer's small order gets executed due to the price move induced by the artificially increased volume
%     on the opposite side of the book
%     \item The spoofer cancels the large non bona fide order immediately after the execution
%     \begin{figure}[htbp]
%     \includegraphics[width=\textwidth]{images/spoofer.pdf}
%     \caption{Timeline of a spoofing episode}
%     \end{figure}

% \end{enumerate}
% Let $i$ indicate the agent of interest, $V_a$ and $V_b$ represent the volumes of the agent's ask and bid orders, 
% respectively, and $\delta_a$ and $\delta_b$ represent their posting distances from the best quote. We consider 
% the following metric that we call distance-weighted imbalance:
% \begin{equation}
%     \left. \text{imb}_{t}^i = \frac{V_a \delta_a - V_b \delta_b}{V_a + V_b} \right|_i
% \end{equation}
% If we consider a sell intent for the spoofer, we expect $\text{imb}_{t}^i$ to be negative at step 1, to become more 
% negative at step 2, and to return to zero at step 3. The opposite holds for a buy intent.
% The crucial point is to distinguish this situation of imbalance between bid and ask from a rebalncing strategy of the market
% maker. In fact, a market maker can also post a large order on one side of the book and a small order on the other side, 
% but in this case we expect the large order to be close to the best quote and the small order to be far from the best 
% quote, while for a spoofer we seek for the opposite. Hence, in this case the imbalance would be positive and it would move
% towards zero more smoothly as the big order is filled, without a sharp and sudden jump as in the spoofer case.

\subsection{A Spatio-Temporal Framework for Spoofing Detection}
\label{sec:spoofer_timeline}

\begin{figure}[htbp]
    \centering
    \resizebox{0.95\textwidth}{!}{%
    \begin{tikzpicture}[
        x=0.95cm,
        y=0.95cm,
        font=\small,
        book/.style={->, very thick},
        quote/.style={very thick},
        bid/.style={green!65!black},
        ask/.style={orange!85!black},
        fake/.style={fill=green!70!black, draw=green!70!black},
        real/.style={fill=red!80!black, draw=red!80!black},
        layer/.style={draw=black!45, fill=gray!8, rounded corners=2pt, minimum width=0.40cm},
        dist/.style={<->, black!70, very thick}
    ]
        \foreach \xshift/\step in {0/State 1: posturing,7.0/State 2: fill,14.0/State 3: cancellation} {
            \node[font=\large\bfseries] at (\xshift+3.0,3.15) {\step};
            \draw[book] (\xshift,0) -- (\xshift+6.0,0);
            \draw[quote,bid] (\xshift+2.25,-0.12) -- (\xshift+2.25,0.12)
                node[below=0.05cm, font=\bfseries] {best bid};
            \draw[quote] (\xshift+3.00,-0.10) -- (\xshift+3.00,0.10)
                node[above=0.05cm, font=\bfseries] {mid};
            \draw[quote,ask] (\xshift+3.85,-0.12) -- (\xshift+3.85,0.12)
                node[below=0.05cm, font=\bfseries] {best ask};
        }

        % State 1: layered bid-side deceptive profile and small ask-side bona fide order.
        \foreach \x/\h/\lab in {0.75/1.10/{$V^{i,b}_{4}$},1.10/1.55/{$V^{i,b}_{3}$},1.45/1.85/{$V^{i,b}_{2}$},1.80/0.45/{$V^{i,b}_{1}$}} {
            \draw[fake] (\x,0) rectangle +(0.24,\h);
            \node[above, font=\scriptsize] at (\x+0.12,\h+0.05) {\lab};
        }
        \draw[real] (4.22,0) rectangle +(0.24,0.45);
        \node[right, font=\scriptsize] at (4.48,0.30) {$V^{i,a}_{1}$};
        \node[align=center, font=\scriptsize] at (1.45,2.35) {large opposite-side\\top-$n$ profile};
        \node[align=center, font=\scriptsize] at (4.55,1.00) {small bona fide\\order};

        % State 2.
        \foreach \x/\h in {7.75/1.10,8.10/1.55,8.45/1.85,8.80/0.45} {
            \draw[fake] (\x,0) rectangle +(0.24,\h);
        }
        \draw[real] (11.22,0) rectangle +(0.24,0.45);
        \draw[orange!85!black, very thick] (11.12,0.06) -- (11.55,0.50);
        \draw[orange!85!black, very thick] (11.55,0.06) -- (11.12,0.50);
        \node[rotate=45, black!85!black, font=\normalsize\bfseries] at (11.35,0.28) {fill};

        % State 3.
        \foreach \x/\h in {14.75/1.10,15.10/1.55,15.45/1.85,15.80/0.45} {
            \draw[fake] (\x,0) rectangle +(0.24,\h);
        }
        \draw[black, very thick] (14.60,0.05) -- (16.20,1.95);
        \draw[black, very thick] (16.20,0.05) -- (14.60,1.95);
        \node[rotate=48, black, font=\normalsize\bfseries] at (15.40,1.00) {cancel};

        \draw[-{Latex[length=3mm]}, thick, black!75] (6.2,1.4) -- (6.8,1.4);
        \draw[-{Latex[length=3mm]}, thick, black!75] (13.2,1.4) -- (13.8,1.4);
    \end{tikzpicture}%
    }
    \caption{Schematic top-$n$ spoofing timeline. The large non-bona fide order is represented as an agent-specific
    depth profile over several bid levels, while the bona fide order sits close to the opposite best quote. The same
    construction applies symmetrically to sell- and buy-intent episodes.}
    \label{fig:topn_spoofing_timeline}
\end{figure}

To rigorously distinguish manipulative spoofing from legitimate liquidity provision, we define the microstructural
signature of the strategy as a conditional deformation of the agent's limit-order-book footprint. The relevant object is
not only the size of a single order at the best bid or ask, but the whole agent-specific liquidity profile over the first
$n$ price levels. This is important because a sophisticated spoofer may place the non-bona fide order away from the
absolute best quote, for example at Level 2, Level 3, or across several adjacent levels, in order to remain visible while
reducing the probability of unintended execution.

We formalize the execution timeline of a spoofing agent $i$ into three states:
\begin{enumerate}
    \item \textbf{State 1 (Deceptive Posturing):} shortly before the execution, the agent posts a large non-bona fide
    liquidity profile on one side of the book, typically away from the touch, while simultaneously maintaining a smaller
    bona fide order close to the opposite best quote.
    \item \textbf{State 2 (Execution):} the artificial depth profile distorts short-horizon imbalance signals and the
    bona fide order is executed.
    \item \textbf{State 3 (Risk Termination):} immediately after the execution, the agent cancels the opposite-side
    liquidity profile, thereby terminating the execution risk associated with the deceptive orders.
\end{enumerate}

The model below generalizes the best-level construction to the top $n$ levels while preserving the same economic logic:
a large asymmetric profile must be posted shortly before the fill, must be present before the fill, and must disappear
conditionally after the fill. This timing restriction is important: long-lived resting liquidity that happens to be on
the opposite side of a later execution should not be mechanically interpreted as part of a spoofing episode.

\subsubsection{Top-$n$ Agent-Specific Depth Profiles}
\label{subsec:topn_depth_profiles}

Let $s \in \{b,a\}$ denote the bid or ask side of the book, and let $k=1,\ldots,n$ denote the book level, with
$k=1$ corresponding to the same-side best quote. For agent $i$, define
\begin{equation}
    V^{i,s}_{k}(t)
\end{equation}
as the volume posted by the agent at time $t$ on side $s$ and level $k$. We denote by
\begin{equation}
    \mathbf{V}^{i,s}_{n}(t)
    = \left(V^{i,s}_{1}(t),\ldots,V^{i,s}_{n}(t)\right)
\end{equation}
the corresponding top-$n$ agent-level depth vector. In applications where the book depth varies substantially across
assets or intraday periods, the raw volume can be normalized by the total visible market depth at the same side and
level:
\begin{equation}
    \widetilde V^{i,s}_{k}(t)
    =
    \frac{V^{i,s}_{k}(t)}{D^{s}_{k}(t)},
    \label{eq:relative_agent_depth}
\end{equation}
where $D^{s}_{k}(t)$ is the total displayed market depth on side $s$ and level $k$.
The distance of a level from the touch is denoted by $d^{s}_{k}(t)$. In a tick-time perspective, $d^{s}_{k}(t)$ can be
measured as the number of ticks between level $k$ and the same-side best quote, shifted by one unit so that the top level
has positive distance. This avoids assigning zero weight to Level 1 while preserving the ranking of deeper levels.

\subsubsection{Multilevel Distance-Weighted Imbalance}
\label{subsec:multilevel_dwi}

The first ingredient is a scalar projection of the top-$n$ depth profile. We assign to each level a depth kernel
\begin{equation}
    \omega^{s}_{k}(t)
    =
    \frac{
        e^{-\lambda d^{s}_{k}(t)}
        \left(1-e^{-\kappa d^{s}_{k}(t)}\right)
    }{
        \sum_{\ell=1}^{n}
        e^{-\lambda d^{s}_{\ell}(t)}\left(
        1-e^{-\kappa d^{s}_{\ell}(t)}\right)
    },
    \label{eq:depth_kernel}
\end{equation}
where $\kappa>0$ controls the increase in execution-risk protection with distance from the touch, while $\lambda>0$
controls the decay of informational visibility at deeper levels. The product form captures the trade-off faced by a
spoofer: a deceptive order should be sufficiently far from the touch to reduce execution risk, but not so far that it
becomes irrelevant for short-horizon imbalance signals.

The agent's weighted top-$n$ liquidity on side $s$ is then
\begin{equation}
    L^{i,s}_{n}(t)
    =
    \sum_{k=1}^{n}
    \omega^{s}_{k}(t)\,\widetilde V^{i,s}_{k}(t).
    \label{eq:weighted_topn_liquidity}
\end{equation}
We define the \textit{Multilevel Distance-Weighted Imbalance} for agent $i$ as
\begin{equation}
    \operatorname{DWI}^{i}_{n}(t)
    =
    \frac{
        L^{i,a}_{n}(t)-L^{i,b}_{n}(t)
    }{
        L^{i,a}_{n}(t)+L^{i,b}_{n}(t)
    }.
    \label{eq:multilevel_dwi}
\end{equation}
With this sign convention, $\operatorname{DWI}^{i}_{n}(t)>0$ indicates an ask-heavy agent profile, while
$\operatorname{DWI}^{i}_{n}(t)<0$ indicates a bid-heavy agent profile. Because the denominator normalizes by the total
weighted agent footprint, the statistic is comparable across agents and trading periods. The case $n=1$ recovers a
best-level version of the measure, whereas larger $n$ values allow the detector to capture deceptive liquidity placed
behind the touch.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        x=1.55cm,
        y=2.3cm,
        font=\scriptsize,
        axis/.style={->, thick},
        spoof/.style={very thick, red!70!black},
        maker/.style={very thick, dashed, blue!70!black},
        event/.style={gray!70, dashed},
        state/.style={fill=gray!8, draw=gray!45, rounded corners=2pt, align=center}
    ]
        \fill[gray!5] (0,-1.05) rectangle (1.7,1.05);
        \fill[gray!10] (1.7,-1.05) rectangle (3.0,1.05);
        \fill[gray!5] (3.0,-1.05) rectangle (4.6,1.05);

        \draw[axis] (0,-1.05) -- (0,1.1) node[above] {$\operatorname{DWI}_{n}^{i}(t)$};
        \draw[axis] (0,-1.05) -- (4.85,-1.05) node[right] {event time};
        \draw[gray!50] (-0.05,0) -- (4.65,0);

        \foreach \y/\lab in {-1/$-1$,0/$0$,1/$1$} {
            \draw (-0.05,\y) -- (0.05,\y) node[left=0.08cm] {\lab};
        }

        \draw[event] (1.0,-1.05) -- (1.0,1.0);
        \draw[event] (2.2,-1.05) -- (2.2,1.0);
        \draw[event] (3.2,-1.05) -- (3.2,1.0);

        \node[state, text width=1.25cm] at (0.5,0.86) {State 1\\posturing};
        \node[state, text width=1.15cm] at (2.0,0.86) {State 2\\fill};
        \node[state, text width=1.25cm] at (3.9,0.86) {State 3\\cancel};

        \node[align=center] at (1.0,-1.25) {$t_0$\\top-$n$ profile};
        \node[align=center] at (2.2,-1.25) {$t_e$\\small fill};
        \node[align=center] at (3.2,-1.25) {$t_e+\Delta\tau$\\collapse};

        \draw[spoof] (0.15,0) -- (0.9,0)
            -- (1.0,-0.55) -- (2.2,-0.55)
            -- (2.2,-0.82) -- (3.2,-0.82)
            -- (3.2,-0.03) -- (4.55,-0.03);

        \draw[maker] (0.15,0) .. controls (1.0,-0.35) and (2.1,-0.45) .. (3.0,-0.25)
            .. controls (3.7,-0.10) and (4.1,-0.03) .. (4.55,0);

        \node[red!70!black, align=left, anchor=west] at (3.35,-0.72) {spoofing:\\conditional collapse};
        \node[blue!70!black, align=left, anchor=west] at (3.35,0.30) {market maker:\\smooth rebalancing};
        \node[align=center] at (2.35,-1.62) {For the opposite spoofing direction, the sign pattern is mirrored.};
    \end{tikzpicture}
    \caption{Stylized event-time behavior of the multilevel distance-weighted imbalance. A spoofing episode produces a
    large top-$n$ imbalance and, after the small opposite-side execution, a rapid return toward zero when the deceptive
    profile is cancelled. Inventory rebalancing by a market maker should evolve more smoothly.}
    \label{fig:topn_imbalance_dynamics}
\end{figure}
\FloatBarrier

\subsubsection{Conditionality and Intent Isolation}
\label{subsec:multilevel_conditionality}

A static multilevel imbalance is not sufficient to isolate manipulative intent. A legitimate market maker may also
exhibit temporary imbalance while managing inventory or reacting to adverse selection. The discriminating information is
the conditionality of the subsequent cancellation: for a spoofer, the disappearance of the large liquidity profile is
mechanically tied to the execution of the smaller bona fide order on the opposite side.

Let $e$ denote an execution of agent $i$ at timestamp $t_e$, and let $s_e\in\{b,a\}$ be the side of the executed bona
fide order. The suspected deceptive side is the opposite side, denoted by $\bar{s}_e$. We define
\begin{equation}
    t_e^{-}=t_e-\epsilon_t,
    \qquad
    t_e^{+}=t_e+\Delta\tau,
\end{equation}
where $\epsilon_t$ is chosen so that $t_e^{-}$ represents the book state immediately before the execution, while
$\Delta\tau$ bounds the mechanical latency of the cancellation response.
We also impose a pre-execution episode window $\Theta>0$. An opposite-side order is considered part of the candidate
deceptive profile for execution $e$ only if its first observed posting time $\tau_o$ satisfies
\begin{equation}
    0 \leq t_e-\tau_o \leq \Theta .
    \label{eq:deceptive_age_window}
\end{equation}
In the empirical implementation below we use $\Theta=10$ minutes as an exploratory default. The candidate deceptive
orders used for episode attribution are therefore restricted to same-client, opposite-side top-$n$ orders posted within
this pre-execution window. The unrestricted top-$n$ state remains useful for describing the agent's book footprint and
for computing DWI and side-collapse statistics, but the intent-isolation step should not attribute old standing liquidity
to the spoofing episode.

The multilevel analogue of the Spoofing Conditionality Index is
\begin{equation}
    \operatorname{SCI}^{i}_{e,n}
    =
    \left|
        \operatorname{DWI}^{i}_{n}(t_e^{-})
        -
        \operatorname{DWI}^{i}_{n}(t_e^{+})
    \right|.
    \label{eq:topn_sci}
\end{equation}
However, the top-$n$ representation allows us to be more selective than a pure imbalance difference. We can directly
measure how much of the weighted liquidity on each side disappears after the fill:
\begin{equation}
    C^{i,s}_{e,n}
    =
    \frac{
        \left[
            L^{i,s}_{n}(t_e^{-})-L^{i,s}_{n}(t_e^{+})
        \right]_{+}
    }{
        L^{i,s}_{n}(t_e^{-})+\varepsilon
    },
    \qquad s\in\{b,a\},
    \label{eq:side_collapse}
\end{equation}
where $[x]_{+}=\max\{x,0\}$. A spoofing episode should produce a large value of $C^{i,\bar{s}_e}_{e,n}$, because the
liquidity that vanishes is expected to be on the side opposite to the bona fide execution. By contrast, a general
market-maker quote refresh induced by a mid-price change should tend to affect both sides more symmetrically.

We therefore define the \textit{Multilevel Spoofing Conditionality Index} as
\begin{equation}
    \operatorname{MSCI}^{i}_{e,n}
    =
    \operatorname{SCI}^{i}_{e,n}\,
    C^{i,\bar{s}_e}_{e,n}\,
    \left[
        C^{i,\bar{s}_e}_{e,n}-C^{i,s_e}_{e,n}
    \right]_{+}.
    \label{eq:msci}
\end{equation}
This score is high only when three conditions hold simultaneously: the agent's top-$n$ imbalance changes abruptly, the
collapse occurs on the side opposite to the execution, and the opposite-side collapse is stronger than the same-side
collapse. This construction preserves the interpretability of the original SCI while reducing false positives generated
by ordinary two-sided quote updates.

\subsubsection{Multilevel Conditional Probability Score}
\label{subsec:mcps}

To aggregate repeated behavior over a session, let $N_i$ be the number of relevant small-order executions by agent $i$.
For a fixed depth $n$, we define the \textit{Multilevel Conditional Probability Score} as
\begin{equation}
    \operatorname{MCPS}^{i}_{n}
    =
    \frac{1}{N_i}
    \sum_{e=1}^{N_i}
    \mathbb{I}\left(
        \operatorname{MSCI}^{i}_{e,n}>\gamma_n
    \right),
    \label{eq:mcps}
\end{equation}
where $\gamma_n$ is an empirically calibrated threshold for the selected book depth. This is the direct multilevel
analogue of the original CPS: it counts the fraction of executions after which the agent exhibits a sufficiently large
conditional, opposite-side collapse of its top-$n$ liquidity profile.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        x=0.72cm,
        y=2.45cm,
        font=\scriptsize,
        axis/.style={->, thick},
        threshold/.style={gray!70, dashed, thick},
        makerpoint/.style={circle, fill=blue!70!black, draw=blue!70!black, inner sep=1.7pt},
        spoofpoint/.style={circle, fill=red!70!black, draw=red!70!black, inner sep=1.7pt},
        barbox/.style={draw, rounded corners=2pt, fill=gray!6}
    ]
        \draw[axis] (0,0) -- (10.9,0);
        \draw[axis] (0,0) -- (0,1.25) node[above] {$\operatorname{MSCI}^{i}_{e,n}$};
        \draw[threshold] (0,0.72) -- (10.6,0.72) node[right] {$\gamma_n$};
        \node[align=center] at (5.35,-0.20) {execution index $e$};

        \foreach \x/\lab in {1/1,5/5,10/$N_i$} {
            \draw (\x,0.03) -- (\x,-0.03) node[below] {\lab};
        }
        \foreach \y/\lab in {0.72/$\gamma_n$,1.0/$1$} {
            \draw (0.03,\y) -- (-0.03,\y) node[left] {\lab};
        }

        \node[blue!70!black, anchor=west] at (0.15,1.18) {market maker: rare threshold crossings};
        \node[red!70!black, anchor=west] at (0.15,1.07) {spoofing: repeated threshold crossings};

        \foreach \x/\y in {1/0.22,2/0.35,3/0.18,4/0.45,5/0.28,6/0.78,7/0.31,8/0.24,9/0.40,10/0.27} {
            \node[makerpoint] at (\x,\y) {};
        }
        \foreach \x/\y in {1/0.82,2/0.95,3/0.88,4/0.67,5/1.02,6/0.91,7/0.76,8/0.99,9/0.85,10/0.93} {
            \node[spoofpoint] at (\x,\y) {};
        }

        \draw[barbox] (11.55,-0.34) rectangle (14.25,1.02);
        \draw[fill=blue!70!black] (12.00,0.08) rectangle (12.35,0.18);
        \draw[fill=red!70!black] (13.45,0.08) rectangle (13.80,0.88);
        \node[align=center] at (12.90,1.14) {$\operatorname{MCPS}^{i}_{n}$};
        \node[blue!70!black, align=center, font=\tiny] at (12.18,-0.14) {market\\maker};
        \node[red!70!black, align=center, font=\tiny] at (13.63,-0.14) {spoofer};

        \node[align=center, text width=0.85\textwidth] at (6.4,-0.55)
        {$\operatorname{MCPS}^{i}_{n}$ counts the fraction of executions for which the multilevel conditionality score
        exceeds $\gamma_n$. High values indicate repeated conditional cancellation after fills.};
    \end{tikzpicture}
    \caption{Operational interpretation of the Multilevel Conditional Probability Score. Each point is one execution for
    the agent. The score increases when the multilevel conditionality index repeatedly exceeds the threshold $\gamma_n$.}
    \label{fig:mcps_score}
\end{figure}
\FloatBarrier

In practice, the depth $n$ should be chosen according to the venue, tick size, and typical liquidity distribution of the
instrument. A robust surveillance implementation can compute MCPS on a grid $\mathcal{N}=\{1,2,3,5,10\}$ and
retain the resulting vector
\begin{equation}
    \left(
    \operatorname{MCPS}^{i}_{1},
    \operatorname{MCPS}^{i}_{2},
    \operatorname{MCPS}^{i}_{3},
    \operatorname{MCPS}^{i}_{5},
    \operatorname{MCPS}^{i}_{10}
    \right)
\end{equation}
as an interpretable multiscale alert profile. A spoofer operating close to the touch should be detected at small $n$,
whereas a strategy that layers deceptive volume deeper in the book should produce larger values only once deeper levels
are included.

\subsubsection{Event Reconstruction}
\label{subsec:event_reconstruction}

The above quantities should be computed from order-level data rather than from aggregate snapshots whenever possible.
The reason is that a resting order may move from one level to another because the best quote changes, even if the agent
has not cancelled or modified the order. For intent isolation, the relevant event is the removal of agent-owned
liquidity after the execution of the bona fide order. The operational procedure is therefore:
\begin{enumerate}
    \item reconstruct the active orders of agent $i$ immediately before $t_e$;
    \item assign each active order to its side $s$ and level $k\leq n$, retaining as candidate deceptive orders only
    those on the opposite side whose first observed posting time lies in $[t_e-\Theta,t_e]$;
    \item compute $L^{i,b}_{n}(t_e^{-})$, $L^{i,a}_{n}(t_e^{-})$, and $\operatorname{DWI}^{i}_{n}(t_e^{-})$;
    \item update the book through the interval $[t_e,t_e+\Delta\tau]$ using executions, cancellations, and amendments;
    \item compute the corresponding post-event quantities at $t_e^{+}$;
    \item evaluate $\operatorname{MSCI}^{i}_{e,n}$ and aggregate it through $\operatorname{MCPS}^{i}_{n}$.
\end{enumerate}
This order-level reconstruction prevents ordinary repricing of the book from being mechanically interpreted as
cancellation of deceptive liquidity, thereby reducing false positives for legitimate market makers.


% We propose two agent-specific metrics based on the
% microprice and the volume-distance correlation. 
% \paragraph{Agent-Specific Microprice ($m_i$)} The traditional microprice is widely 
% recognized in high-frequency trading as a robust estimator of the expected future mid-price \cite{Stoikov02122018}, uniquely synthesizing both 
% the price and volume dimensions of the limit order book. By isolating this calculation to a single participant's resting 
% orders, we can formulate an an agent-specific microprice ($m_i$) to quantify its personal vision of the future price::

% \begin{equation}
%     m_i = \frac{a_iV^b_i + b_i V^a_i}{V^b_i + V^a_i} = \frac{a_i + b_i}{2} + \frac{a_i-b_i}{2} \frac{V_i^b-V_i^a}{V_i^b+V_i^a} = \text{mid-price} + \frac{\text{spread}}{2} \times \text{imbalance}
% \end{equation}
% Where $a_i$ and $b_i$ represent the agent's ask and bid prices, and $V^a_i$ and $V^b_i$ represent their respective 
% volumes. Legitimate market makers, constrained by inventory risk, typically maintain symmetric quotes ($V^b_i \approx V^a_i$) 
% to capture the spread, resulting in an $m_i$ that closely tracks the fundamental mid-price. Conversely, a spoofer 
% deliberately engineers an extreme unbalanced environment to manipulate the heuristics of victim algorithms. 
% Consequently, a spoofer's $m_i$ will drastically diverge from the true mid-price.

% \paragraph{Correlation Between Posting Distance ($\delta$) and Volume} The second metric mathematically formalizes the 
% spoofer's optimization dilemma by measuring the correlation between an agent's order volume and their posting distance 
% ($\delta$) from the best bid or ask. Empirical models confirm that posting distance is a critical determinant of price 
% impact, and its omission renders LOB detection models mathematically inadequate.Because market makers seek execution to 
% earn the bid-ask spread, their inventory management necessitates posting their largest order volumes at or adjacent to 
% the top of the book ($\delta \approx 0$). Spoofers, conversely, optimize a strictly directional expected cost function: 
% they must maximize the psychological and algorithmic impact of their non-bona fide volume while minimizing the risk of 
% inadvertent execution. This is achieved by placing a small, bona fide order at a minimal distance ($\delta \approx 0$) 
% to guarantee execution, and a massive, non-bona fide order at a significantly larger distance deeper within the opposite 
% side of the LOB ($\delta \gg 0$) to act as a structural buffer against aggressive market orders. Thus, observing a strong 
% correlation between massive volume and a large $\delta$ on one side of the book—paired with small volume and a minimal 
% $\delta$ on the execution side—serves as and additional microstructural signature of a manipulative strategy.


\bibliographystyle{abbrv}
\bibliography{references}

\end{document}
